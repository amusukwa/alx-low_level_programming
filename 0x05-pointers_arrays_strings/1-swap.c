#include "main.h"
/**
 * swap_int - swaps the values of two integers
 *
 * @a: first input as integer
 * @b: second input as integer
 * Return:no return type
 */
void swap_int(int *a, int *b)
{
	m = *a;

	*a = *b;
	*b = m;
}
