#include "main.h"
/**
 * void print_alphabet(void) - a program that prints the alphabet in lowercase
 *
 * Return: 0 Always(Success)
 */
void print_alphabet(void)
{
	int i;

	for (i = 97; i <= 1222; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
