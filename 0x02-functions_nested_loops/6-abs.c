#include "main.h"
/**
 *  _abs(int)- Computes absolute value
 *  of an integer
 *
 *  @i: input member as integer
 *
 *  Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

