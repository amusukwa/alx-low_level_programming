#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 Always(Success)
 *
 */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
		putchar(',');
		putchar(',');
	}
	for (m = 48; m <= 57; m++)
	{
	}
	putchar('\n');
	return (0);
}
