#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 Always(Success)
 *
 */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
		putchar(',');
		putchar(',');
		putchar(',');
	}
	putchar('\n');
	return (0);
}
